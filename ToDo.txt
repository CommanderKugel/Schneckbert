functionality

- Mailbox (faster lookups)
- FRC (castling, fen parsing, DFRC)
- Staged Movegen (ttMove first, staged Capture & quiets)
- move_gives_check()
- Multithreading (big rewrite x.x)

fury-pasta

Search heuristics:
- Improving heuristic
    - RFP (doesnt gain)
    - LMR (R+=!improving; doesnt gain, interaction with log-formula pending)
    - LMP (movesToGo /= 2, -bizillion elo :( )
- Continuation 
- Capture history heuristic
- History pruning/reduction
- Multicut (using singular search result)
- Double/Triple/Negative extensions
- Cutnode (as apart of negative extensions, LMR, etc)
- Static eval correction history
- QS futility pruning

Time management for soft-bound:
- Node-based scaling
- Best move stability
- Eval stability

viri-pasta

NNUE progression:
- Fused Updates
- Direct accumulator calculation (accumulators[ply] = accumulators[ply - 1] + changes)
- Lazy Updates (Queue of accumulator updates, calculate only before evaluation)
- Finny Tables (For each bucket, store the state of the last time you were there, and when you re-enter the bucket, use a bitboard diff to compute updates)
- multilayer (but first get to 1024 ft size)
