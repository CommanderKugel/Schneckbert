ORIGINAL IDEAS:
- score becomes i32 struckt packing: cp/mate-score (i16), win% (i8), loss% (i8)
    -> win/loss scaled from % to 255, draw = 255 - win - loss
    -> requires wdl-output from NNUE instead of plain cp-score
    -> different score conditions based on wdl instead of score alone    
    -> to CentiPawns, use elo formula or something similar (win + draw/2) / 255

- HCE-like input features
    - PassedPawns plane (2 * 7 PieceTypes * 64 squares => 896 features)
    - KingAttacker plane (King Zone is under Attack) (2 * (2 * 6) = 1.536 features)

- prob-tt-Cut for ttDepth - 1 >= depth
    - allnode && upperBound && score + margin < alpha; or respectively cutnode && lowerBound && score - margin > beta
        -> TT-Cutoff

- NNUE Output Buckets based on Material imbalances (e.g. no queens, knight vs bishop)
- NNUE Output Buckets based on phase values instead of blocker_count [1, 2, 2, 4, 8, 0] or [0, 1, 1, 2, 4, 0]

- 'Complexity' = PESTO vs NNUE difference for tweaks like improving (or even NNUE psqt)


COMMON FUNCTIONALITY:
- Add Assertions for all kinds of stuff

- Mailbox (faster lookups)
- FRC (castling, fen parsing, pos from Number, DFRC)
- Staged Movegen 
    - staged Capture & quiets
    - noisy-picker including checks
        - move_gives_check()
- Multithreading (big rewrite x.x)


=== fury-pasta ===

Search heuristics:
- Improving heuristic
    - LMR 
    - LMP 
- History Related Stuff 
    - Continuation- & CounterHistory
    - Capthist: Butterfly -> PieceTo
    - Hist-pruning
    - Hist-reductions
- Singular Confirmation Search Related
    - Multicut
    - Double/Triple extensions
    - Negative extensions (score<alpha, score<beta, cutnode)
- Cutnode (as apart of negative extensions, LMR, etc)
- Static eval correction history (HELP - its fucked)
    - Pawn
    - Minor/Major
    - Material
    - Continuation
- QS futility pruning
- Prob Cut (dont waste 93487623 hours like yukari plz)

Time management for soft-bound:
- Node-based scaling
- Eval stability


=== viri-pasta ===

NNUE progression:
- (Accumulator Stack)
- Fused Updates
- Direct accumulator calculation (accumulators[ply] = accumulators[ply - 1] + changes)
- Lazy Updates (Queue of accumulator updates, calculate only before evaluation)
- Finny Tables (For each bucket, store the state of the last time you were there, and when you re-enter the bucket, use a bitboard diff to compute updates)
- multilayer (but first get to 1024 ft size)
