

public struct SS
{
    public byte MovedPiece;
    public byte CapturedPiece;
    public move Move;
}

public static class SearchStack
{
    // ToDo -> until now everything is in a pos-struct but that is inaccessible
    // and gets very simply passed down the search recursion
}
